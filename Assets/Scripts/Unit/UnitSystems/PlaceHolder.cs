using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaceHolder 
{

    protected void Update()
    {
    

      //  _firePrimary = (Input.GetKeyDown(KeyCode.Space) || Input.GetMouseButtonDown(0)) ? true : false;

       // _fireSecondary = (Input.GetKeyDown(KeyCode.LeftAlt) || Input.GetMouseButtonDown(1)) ? true : false;

    }

}
